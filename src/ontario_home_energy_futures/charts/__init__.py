"""Accessible, dependency-free chart generation.

See ACCESSIBILITY.md's "Accessibility decision record: charts and dynamic
results": every chart is hand-built inline SVG (no third-party charting
library), paired with an accessible HTML data table containing the same
numbers, and never relies on colour alone to distinguish series.
"""
